# Compute confidence interval

Compute confidence interval

## Usage

``` r
compute_confidence_inteval(vector)
```

## Arguments

- vector:

  vector

## Examples

``` r
set.seed(1)
vector<-rnorm(1000)
compute_confidence_inteval(vector)
#> [1] 0.06414357
```
