% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUNCTIONS_STRINGR.R, R/FUNCTIONS_STRINGS.R
\name{str_replace}
\alias{str_replace}
\title{Replace the first pattern match in a string}
\usage{
str_replace(string, pattern, replacement)

str_replace(string, pattern, replacement)
}
\arguments{
\item{string}{A character vector.}

\item{pattern}{A regular expression string or a literal string wrapped in
\code{fixed()}.}

\item{replacement}{A character string to replace the first match with.}
}
\value{
A character vector the same length as \code{string}.

A character vector the same length as \code{string}.
}
\description{
Replaces only the first occurrence of \code{pattern} in each
  element of \code{string}. For replacing all occurrences use
  \code{str_replace_all}.

Replaces only the first occurrence of \code{pattern} in each
  element of \code{string}. For replacing all occurrences use
  \code{str_replace_all}.
}
\examples{
# Only the first "o" is replaced
str_replace("hello world", "o", "0")

# Remove leading zero (first match only)
str_replace("007 bond", "^0+", "")

# Fixed match: replace first literal dot
str_replace("a.b.c", fixed("."), "-")
# Only the first "o" is replaced
str_replace("hello world", "o", "0")

# Remove leading zero (first match only)
str_replace("007 bond", "^0+", "")

# Fixed match: replace first literal dot
str_replace("a.b.c", fixed("."), "-")
}
\keyword{strings}
