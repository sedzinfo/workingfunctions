% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUNCTIONS_STRINGR.R, R/FUNCTIONS_STRINGS.R
\name{str_count}
\alias{str_count}
\title{Count the number of pattern matches in a string}
\usage{
str_count(string, pattern)

str_count(string, pattern)
}
\arguments{
\item{string}{A character vector.}

\item{pattern}{A regular expression string or a literal string wrapped in
\code{fixed()}.}
}
\value{
An integer vector the same length as \code{string}.

An integer vector the same length as \code{string}.
}
\description{
Returns the number of times \code{pattern} appears in each
  element of \code{string}. Supports both regular expressions and literal
  string matching via \code{fixed()}.

Returns the number of times \code{pattern} appears in each
  element of \code{string}. Supports both regular expressions and literal
  string matching via \code{fixed()}.
}
\examples{
# Count vowels
str_count(c("banana", "apple", "cherry"), "[aeiou]")

# Count literal semicolons (useful for delimited data)
str_count(c("a;b;c", "x;y", "z"), fixed(";"))

# Count digits
str_count(c("abc123", "99bottles", "none"), "[0-9]")
# Count vowels
str_count(c("banana", "apple", "cherry"), "[aeiou]")

# Count literal semicolons (useful for delimited data)
str_count(c("a;b;c", "x;y", "z"), fixed(";"))

# Count digits
str_count(c("abc123", "99bottles", "none"), "[0-9]")
}
\keyword{strings}
