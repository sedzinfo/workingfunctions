% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUNCTIONS_STRINGR.R, R/FUNCTIONS_STRINGS.R
\name{str_pad}
\alias{str_pad}
\title{Pad a string to a minimum width}
\usage{
str_pad(string, width, side = "right", pad = " ")

str_pad(string, width, side = "right", pad = " ")
}
\arguments{
\item{string}{A character vector.}

\item{width}{Integer. Minimum total width of the output string.}

\item{side}{One of \code{"right"} (default), \code{"left"}, or \code{"both"}.}

\item{pad}{A single character to use for padding. Default \code{" "}.}
}
\value{
A character vector the same length as \code{string}.

A character vector the same length as \code{string}.
}
\description{
Pads \code{string} with \code{pad} characters on the left,
  right, or both sides until it reaches at least \code{width} characters.
  Strings already at or exceeding \code{width} are returned unchanged.

Pads \code{string} with \code{pad} characters on the left,
  right, or both sides until it reaches at least \code{width} characters.
  Strings already at or exceeding \code{width} are returned unchanged.
}
\examples{
# Zero-pad single digit numbers on the left
str_pad(c("1", "10", "100"), width=3, side="left", pad="0")

# Right-pad to align labels
str_pad(c("Name", "Age", "Score"), width=10)

# Pad on both sides (centers the string)
str_pad("hello", width=11, side="both")
# Zero-pad single digit numbers on the left
str_pad(c("1", "10", "100"), width=3, side="left", pad="0")

# Right-pad to align labels
str_pad(c("Name", "Age", "Score"), width=10)

# Pad on both sides (centers the string)
str_pad("hello", width=11, side="both")
}
\keyword{strings}
