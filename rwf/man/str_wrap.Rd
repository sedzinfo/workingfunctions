% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUNCTIONS_STRINGR.R, R/FUNCTIONS_STRINGS.R
\name{str_wrap}
\alias{str_wrap}
\title{Wrap long strings to a specified line width}
\usage{
str_wrap(string, width = 80)

str_wrap(string, width = 80)

str_wrap(string, width = 80)
}
\arguments{
\item{string}{A character vector.}

\item{width}{Maximum number of characters per line. Default \code{80}.}

\item{x}{Character. The string to wrap.}

\item{...}{Additional arguments passed to \code{\link[base]{strwrap}},
such as \code{width}.}
}
\value{
A character vector the same length as \code{string}, with embedded
  newlines inserted at word boundaries.

A single character string with newlines inserted at wrap points.

A character vector the same length as \code{string}, with embedded
  newlines inserted at word boundaries.
}
\description{
Breaks a character string into multiple lines so that no line
  exceeds \code{width} characters. Words are kept intact; lines are joined
  with \code{"\n"}.

Wraps a character string at a given width and collapses the 
result into a single newline-delimited string. Useful for formatting long
plot titles or labels.

Breaks a character string into multiple lines so that no line
  exceeds \code{width} characters. Words are kept intact; lines are joined
  with \code{"\n"}.
}
\examples{
# Wrap at 30 characters
cat(str_wrap("The quick brown fox jumped over the lazy dog", width=30))

# Wrap a vector of strings
labels <- c("Short label", "A much longer label that needs wrapping")
str_wrap(labels, width=20)
str_wrap(rep("sting",50),30)
# Wrap at 30 characters
cat(str_wrap("The quick brown fox jumped over the lazy dog", width = 30))

# Wrap a vector of strings
labels <- c("Short label", "A much longer label that needs wrapping")
str_wrap(labels, width = 20)
}
\keyword{strings}
