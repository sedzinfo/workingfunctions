% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUNCTIONS_PLOT.R
\name{duplicate_y_axis}
\alias{duplicate_y_axis}
\title{Duplicate the y axis on the right side of a ggplot}
\usage{
duplicate_y_axis(p1, p2)
}
\arguments{
\item{p1}{A ggplot object. This plot is drawn with the duplicated right axis.}

\item{p2}{A ggplot object whose left y axis is mirrored to the right of
\code{p1}. Typically the same as \code{p1}.}
}
\value{
Invisibly returns \code{NULL}. The combined plot is drawn to the
  current graphics device.
}
\description{
Takes two ggplot objects and renders \code{p1} with the y axis
  of \code{p2} mirrored onto the right side. Useful when overlaying two
  series with different scales or simply to frame the plot with matching
  axes on both sides.
}
\examples{
p1 <- ggplot(ChickWeight, aes(x=Time, y=weight, colour=Diet, group=Chick)) +
  geom_line() +
  ggtitle("Growth curve for individual chicks")
duplicate_y_axis(p1=p1, p2=p1)
}
\keyword{functions}
\keyword{plot}
